{
  "name": "board-games",
  "version": "0.1.0",
  "description": "Board game collection + private for-sale / bidding page. Stateless magic-link auth.",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "worker": "bun run src/worker/index.ts",
    "sync": "SYNC_ONCE=1 bun run src/worker/index.ts",
    "repoint-covers": "bun run src/worker/repoint-covers.ts"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.21.0",
    "hono": "^4.6.14",
    "sharp": "^0.35.3",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
