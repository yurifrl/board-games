{
  "name": "board-games",
  "version": "0.1.0",
  "description": "Board game collection + private for-sale / bidding page. Stateless magic-link auth.",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "worker": "bun run src/worker/index.ts",
    "worker-sync-once": "SYNC_ONCE=1 bun run src/worker/index.ts"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.21.0",
    "hono": "^4.6.14",
    "markdown-it": "^14.3.0",
    "sharp": "^0.35.3",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/markdown-it": "^14.1.2"
  }
}
