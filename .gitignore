node_modules
.env
.env.old
*.log
*.jsonl
data

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# local GCS service-account key for `bun run sync`
.secrets/

# Google service-account key (never commit)
*-key.json
gcp-*.json
