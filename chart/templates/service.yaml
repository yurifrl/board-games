apiVersion: v1
kind: Service
metadata:
  name: {{ include "board-games.fullname" . }}
spec:
  selector: { app: {{ include "board-games.fullname" . }} }
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
